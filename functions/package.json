{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "npm run build && npm run test:run",
    "test:run": "node test/rest-router.test.js && node test/apple-id-token.test.js && node test/apple-auth.test.js && node test/github-auth.test.js && node test/todo-deletion.test.js && node test/push-notification.test.js",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/tasks": "^6.2.1",
    "axios": "^1.13.6",
    "dotenv": "^16.6.1",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.2",
    "jwks-rsa": "^3.2.0",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "firebase-functions-test": "^3.1.0",
    "typescript": "^4.9.5"
  },
  "private": true
}
